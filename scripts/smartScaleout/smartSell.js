const exchange = require('./exchange.js')

const smartSell = {}

smartSell.start = async ({}) => {

  // get orderbook best sell price
  

  // place order at best sell price

  // on price goes through sell order price, get orderbook best sell price, create new order

  function getPrice(){
    exchange.getPrice()
  }

}